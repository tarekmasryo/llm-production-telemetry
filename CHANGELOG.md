# Changelog

## v1.0.1
- Refactored CLI scripts to small OOP components (generator/validator/runner) without changing CLI flags.
- Applied ruff auto-fixes to the notebook (imports + formatting).

## v1.0.0
- Production packaging: repo structure, CI, pre-commit, pinned deps.
- Added sample telemetry generator and integrity validator.
- Added headless notebook runner with YAML-configurable policy knobs.
